<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Board">
	<!-- 전체 게시글 개수 -->
	<select id="getBoardCnt" parameterType="string"
		resultType="_int">
		select count(*) from ${b_name}
	</select>
	<select id="getBoard" parameterType="hashmap"
		resultType="BoardDTO">
		select
		b.b_index,
		b.b_reg_date,
		b.b_update_date,
		b.b_subject,
		b.b_title,
		b.b_contents,
		b.b_hits,
		b.b_like_user_index,
		b.b_inactive,
		u.user_index,
		u.user_nickname
		from ${b_name} b
		join t_user u
		on b.user_index=u.user_index
		where b.b_inactive = 0 
		order by 1 desc
		limit #{startRow}, #{pageSize}
	</select>

	<insert id="postOn" parameterType="boardDTO">
		insert into
		${b_name}(user_index, b_title, b_contents, b_files)
		values(#{user_index}, #{b_title}, #{b_contents},#{b_files})
	</insert>
	<delete id="postOff" parameterType="hashmap">
		delete from ${b_name} where
		b_index=#{b_index}
	</delete>
	<select id="getPost" parameterType="hashmap"
		resultType="BoardDTO">
		select b.b_index,b.b_reg_date,b.b_update_date,b.b_subject,b.b_title,b.b_contents,b.b_hits,b.b_like_user_index,b.b_inactive,b.b_files,u.user_index,u.user_nickname 
		from ${b_name} b 
		join t_user u 
		on b.user_index=u.user_index
		where b.b_index=#{b_index};
	</select>
	<update id="updateHits" parameterType="hashmap">
		update ${b_name} set
		b_hits=b_hits+1 where b_index=#{b_index}
	</update>

	<select id="getBoardListOnMain" parameterType="string" resultType="BoardDTO">
		select * from
		${b_name} order by 1 desc limit 0,6
	</select>
</mapper>