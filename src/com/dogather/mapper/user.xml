<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="User">
	<select id="selectUser" parameterType="string" resultType="_int">
		select user_index from t_test_user where user_id = #{user_id}
	</select>
	<select id="getTermsList" resultType="TermsDTO">
		
	</select>
 </mapper>